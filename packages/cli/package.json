{
  "name": "notecode",
  "version": "1.0.0",
  "license": "MIT",
  "description": "",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "tsc --watch --preserveWatchOutput",
    "prepublishOnly": "esbuild src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.env.NODE_ENV=\\\"produnction\\\""
  },
  "dependencies": {
    "@notecode/local-client": "^1.0.0"
  },
  "devDependencies": {
    "@notecode/local-api": "^1.0.0",
    "@types/node": "^20.12.7",
    "commander": "^12.0.0",
    "esbuild": "0.8.26",
    "typescript": "^5.4.5"
  }
}
